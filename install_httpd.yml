---


- name: Install httpd an php package
  hosts: web_server
  become: true

  vars: 
    httpd_package: httpd
    php_package: php
  tasks:

    - name: Install
      dnf:
        name:
          - "{{ httpd_package }}"
          - "{{ php_package }}"
        state: latest
        update_cache: yes
